- hosts: all
  tasks:
    - name: Hello Message JUN
      debug:
        msg: "Hello World JUN!"
        
    - name: Check if Apache is running
      systemd:
        state: started
        name: named

- name: Hello World JUN Sample
  tasks:
    - name: Hello Message JUN
      hosts: all
      debug:
        msg: "Hello World JUN!"
        
    - name: Check if Apache is running
      hosts: all
      shell: pgrep apache2
      ignore_errors: yes
      changed_when: false
      register: service_apache_status       

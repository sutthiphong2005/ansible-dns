- hosts: all
  tasks:
    - name: Hello Message JUN
      debug:
        msg: "Hello World JUN!"
        
    - name: Check if Named is running
      systemd:
        state: started
        name: named
